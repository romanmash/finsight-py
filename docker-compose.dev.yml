services:
  api:
    environment:
      RELOAD: "true"
    volumes:
      - ./apps/api-service/src:/app/src
      - ./config/runtime:/app/config/runtime:ro

  worker-watchdog:
    volumes:
      - ./apps/api-service/src:/app/src
      - ./config/runtime:/app/config/runtime:ro

  worker-brief:
    volumes:
      - ./apps/api-service/src:/app/src
      - ./config/runtime:/app/config/runtime:ro
